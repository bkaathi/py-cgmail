machine:
    python:
        version: 2.7.9

test:
    override:
        - py.test test

dependencies:
    pre:
       - pip install pytest
       - sudo apt-get install libxml2-dev libxslt1-dev libxml2
